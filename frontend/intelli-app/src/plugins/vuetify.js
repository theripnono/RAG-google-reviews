import 'vuetify/styles';
import { createVuetify } from 'vuetify';
d

// Create Vuetify instance with desired options
const vuetify = createVuetify({
    icons: {
        defaultSet: 'mdi',
    },
});

export default vuetify; // Export the Vuetify instance
